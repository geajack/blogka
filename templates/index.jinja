{% extends "base.jinja" %}

{% block body %}
<div>
    {% for slug in articles %}
        <a href="/{{slug}}">
            <div class="article">
                {{slug}}
            </div>
        </a>
    {% endfor %}
</div>
{% endblock body %}